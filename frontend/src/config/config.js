const host = 'http://localhost:3000';

const config = {
    host: host,
    api: host + '/api',
    typeOperation: {
        income: 'income',
        expense: 'expense'
    }
}

export default config;